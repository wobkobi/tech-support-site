// src/middleware.ts
/**
 * @file middleware.ts
 * @description
 * Optional middleware for rate-limiting, bot filtering, or header hardening.
 */

// quick file to stop next.js from complaining about no middleware file

/**
 * Does nothing. Placeholder to satisfy Next.js requirement for a middleware file.
 */
export default function middleware(): void {
  return;
}
